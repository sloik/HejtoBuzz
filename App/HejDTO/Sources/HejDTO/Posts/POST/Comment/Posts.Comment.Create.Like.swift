
import Foundation

extension Posts {
    
    /// There is no DTO for this path. This enum is for documentation and to
    /// mark this path as already "done" in the DTOs.
    enum CommentCreateLike {
        
    }
}
